name: CI

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: vfs-link-ci
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  test-and-build:
    name: Test and build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: 1.23.x
          cache-dependency-path: apps/file-server/go.sum
          cache: false

      - name: Test file server
        working-directory: apps/file-server
        run: ./scripts/go.sh test ./...

      - name: Build file server image
        run: |
          set -euo pipefail
          docker build \
            -f apps/file-server/Dockerfile \
            -t vfs-link/file-server:test \
            .
