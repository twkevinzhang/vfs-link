# Deployment preflight — 2026-08-09 21:34 CST

Status: APPROVED; historical revision cleanup and deployment authorized.

Cleanup result (2026-08-09): all 19 approved revisions were deleted. Immediate re-audit: 73 revisions retained, `minScale > 0` count is 0; current receiving revision, traffic, automatic scaling, CPU throttling, min 0, max 12, CPU and memory are unchanged.

Release gate result: `go test ./...`, `go build ./...`, `go vet ./...`, and race tests for upload/share/fileops/webdav/vfs/ftp passed from commit `34ec7c3`.

Cloud Build started from a clean `git archive` of `34ec7c3`:

- Build ID: `65d62a48-45dc-405c-84bd-235c2a389aff`
- Image tag: `asia-east1-docker.pkg.dev/storage-403503/vfs-link/file-server:git-34ec7c3-20260809t1342z`
- Platform timeout: 1200 seconds
- Owner: primary Codex agent; cancel on timeout/failure before any retry

Cloud Build completed successfully in approximately 3 minutes 55 seconds. Image digest: `sha256:ddb134863c55ed5ed11b77c24ab2b28467e7d821679917dea5fdc43abe7e332d`.

First Cloud Run rollout attempt failed closed before receiving traffic: startup detected 90 nonterminal legacy upload sessions. The failed revision was replaced by rollback revision `vfs-link-file-server-rollback-34ec7c3` using the previous image digest, routed to 100%, and then deleted. Rollback re-audit: Ready, CPU throttling, min 0, max 12, 1 vCPU/512 MiB.

Legacy upload drain status:

- 81 expired, zero-metadata-progress sessions were cleaned. 62 cancelled through the previous release API; 19 whose expired GCS resumable capability returned HTTP 400 were revalidated as terminal `expired` and their upload metadata was removed with exact GCS generation preconditions.
- 9 non-expired sessions were reconciled through the previous release. All contained real partial upload data (initial aggregate about 10.4 GiB of 35.7 GiB), so they were not cancelled. One subsequently reached `complete`; eight remained actively uploading at the last check.

ipproxy rollout did not switch images: clean commit `34ec7c3` fails the required non-root Viewer SPA build and does not produce `build/client/index.html`. The old container/image remained unchanged and healthy; physical-health reported 2767/2767 records healthy. A tracked Viewer build fix requires separate user confirmation under the bug-fix acceptance rule.

The unsuccessful release image digest/tag was removed from Artifact Registry after rollback. Local temporary source/build directories and the temporary metadata copies containing upload capabilities were securely removed. The only deployment-created revision that remains is the Ready rollback revision serving the previous image at 100% traffic with CPU throttling and min 0; no failed/candidate revision, tag, minimum instance, running build, or candidate image remains.

## Release source

- Repository: `/Users/zhangzhenlong/Projects/vfs-link`
- Branch: `twkevinzhang`
- Commit: `34ec7c3`
- Existing user-owned dirty files (must not be committed or overwritten):
  - `.gitignore`
  - `deploy/gcp/deploy-cloud-run.sh`

## Cloud Run current service

- Project/region/service: `storage-403503` / `asia-east1` / `vfs-link-file-server`
- Ready revision receiving traffic: `vfs-link-file-server-00098-4d2`
- Traffic/tags: 100% latest; no URL tags
- Resources: 1 vCPU, 512 MiB, 0 GPU
- Billing: request-based (`cpu-throttling=true`)
- Scaling: automatic; service min 0; revision min 0; max 12
- Concurrency/request timeout: 8 / 3600 seconds
- Aggregate persistent capacity for traffic/tag-accessible revisions: 0 instances, 0 vCPU, 0 GiB
- Cloud Monitoring, previous hour: only current revision reported, peak instance count 1

There are 92 retained revisions. All are Ready. Configuration groups:

- 8: default throttling, min 0, max 10, 1 vCPU/1 GiB
- 1: no throttling, min 0, max 10, concurrency 4, 2 vCPU/1 GiB
- 51: no throttling, min 0, max 10, 1 vCPU/1 GiB
- 9: no throttling, min 12, max 12, 2 vCPU/2 GiB
- 1: no throttling, min 4, max 10, 1 vCPU/1 GiB
- 5: no throttling, min 4, max 4, 2 vCPU/2 GiB
- 3: no throttling, min 8, max 10, 1 vCPU/1 GiB
- 1: no throttling, min 8, max 8, 2 vCPU/2 GiB
- 2: throttling, min 0, max 12, 1 vCPU/512 MiB
- 11: throttling, min 0, max 12, 2 vCPU/2 GiB

Historical revisions with revision-level minScale > 0 (none has traffic or tag):

- `vfs-link-file-server-v4-e2a7b80-m12` (12)
- `vfs-link-file-server-v4-7c210b4-m12` (12)
- `vfs-link-file-server-v4-f8d949f-m12` (12)
- `vfs-link-file-server-v4-c993915-m12` (12)
- `vfs-link-file-server-v4-d363962-m12` (12)
- `vfs-link-file-server-v4-8462f73-m12` (12)
- `vfs-link-file-server-v4-c8964fc-m12` (12)
- `vfs-link-file-server-v4-a5a0033-m12` (12)
- `vfs-link-file-server-v4-2820bd1-m12` (12)
- `vfs-link-file-server-v4-2820bd1-m8` (8)
- `vfs-link-file-server-v4-2820bd1` (4)
- `vfs-link-file-server-v4-ce15b8d` (4)
- `vfs-link-file-server-v4-b1ade1c` (4)
- `vfs-link-file-server-v4-7a517dd` (4)
- `vfs-link-file-server-v4-a290c55-c2m4` (4)
- `vfs-link-file-server-v4-stable-reset` (8)
- `vfs-link-file-server-v4-65ebff3` (8)
- `vfs-link-file-server-v4-8633faa-m8` (8)
- `vfs-link-file-server-v4-8633faa-m4` (4)

These revisions are not currently accessible, but their retained minScale/instance-based settings trigger the project cost stop rule. No new revision may be created until approved cleanup is complete.

## Existing GCP dependencies

- Enabled secret versions exist for HTTP Basic Auth and Telegram.
- Existing buckets: primary `US-EAST5`; metadata/thumbnails/shares `ASIA-EAST1`.
- Active metadata configuration: bucket `storage-403503-vfs-link-metadata-v4-2`, prefix `_vfs-link-v4`, 64 shards, scoped mutation.
- Existing Pub/Sub subscription: `vfs-link-share-worker`; push endpoint targets the service URL; dead-letter max deliveries 10; retry 10–600 seconds.
- Last five Cloud Builds all succeeded in approximately 3–5 minutes.

The deploy script defaults to an older metadata bucket/prefix/mode. Deployment must explicitly preserve the active v4 values and `DRIFT_ENABLED=true`; defaults must not be used.

## Proposed deployment limits

- Preserve current 1 vCPU / 512 MiB / no GPU.
- Request-based billing, CPU throttling, min 0, max 12, concurrency 8.
- 3600-second request timeout; no candidate tag; route 100% to latest only after Ready.
- Cloud build deadline 20 minutes; Cloud Run update deadline 10 minutes each.
- End-to-end GCP stage deadline 45 minutes; total paid-operation deadline 60 minutes.
- No load test, no retry beyond one controlled rerun after diagnosis.
- ipproxy build deadline 20 minutes; rollout 5 minutes; acceptance 15 minutes.

## Cost model

Official Cloud Run Tier 1 request-based list price checked 2026-08-09:

- Active CPU: USD 0.000024/vCPU-second
- Active memory: USD 0.0000025/GiB-second
- Requests: USD 0.40/million
- Cloud Build e2-standard-2: USD 0.006/minute; 2,500 promotional free minutes may apply

Exchange rate: exact 2026-08-09 rate is not verified. The latest official Bank of Taiwan result available to this preflight showed spot sell 32.46 TWD/USD; calculations use a conservative 33 TWD/USD.

At the hard maximum of 12 continuously active instances (1 vCPU, 0.5 GiB):

- Hourly compute: `12 × 3600 × (0.000024 + 0.5 × 0.0000025)` = USD 1.0908 = TWD 36.00
- 24-hour compute: USD 26.1792 = TWD 863.91
- 30-day compute: USD 785.376 = TWD 25,917.41
- Requests add TWD 13.20 per million; network egress/storage are workload-dependent and not finitely bounded by instance count.
- Expected one 5-minute build: USD 0.03 = TWD 0.99 before free tier.

Deployment does not increase the current CPU/memory/max settings and creates no minimum capacity. The 60-minute deployment window remains below the default TWD 100 compute/build budget under the stated no-load-test assumption. Organic production request/egress costs are not deployment-created and remain usage-dependent.

## Rollback and cleanup

- Record old revision/image: `vfs-link-file-server-00098-4d2` / `sha256:c530604fa4f84b76910ba60493d428b0a06fe1efc56c6a061e28cc89d84a1f46`.
- On failure, route traffic exactly back to `vfs-link-file-server-00098-4d2`.
- Remove any deployment-created tag (none planned), ensure service/revision min 0, automatic scaling, CPU throttling.
- Do not delete buckets, metadata prefix, secrets, database, or Pub/Sub production resources.
- ipproxy rollback: restore the recorded previous immutable image digest and rerun compose; do not modify PostgreSQL contents.

## ipproxy preflight

- SSH host reachable; runtime directory, `.env`, compose file, and `services` network exist.
- Active container `vfs-link-ipproxy` is healthy; current image digest begins `sha256:1b6d055c...`.
- Runtime `DATABASE_URL` resolves to database `vfs_link_ipproxy`.
- More than 5 GiB disk space is available.

## Approval record

At 2026-08-09 (Asia/Taipei), the user explicitly approved:

- deleting the 19 historical revisions listed above that have no traffic and no URL tag;
- deploying to GCP project `storage-403503` and ipproxy using the stated limits;
- request-based billing with CPU throttling and zero service/revision minimum instances only.

The user explicitly prohibited pre-starting multiple Cloud Run instances or leaving CPU allocated while waiting for traffic. Any observed `minScale > 0`, instance-based billing, always-allocated CPU, unexpected tag, or persistent capacity is a stop condition requiring immediate cleanup before continuing.

## Viewer fix and resumed deployment — 2026-08-09 22:25 CST

The user explicitly approved fixing the Viewer build and continuing both deployments. The fix was committed on the existing `twkevinzhang` branch as `6d6ade3` (`fix(web): 修正非根路徑 Viewer 建置`). User-owned changes to `.gitignore` and `deploy/gcp/deploy-cloud-run.sh` remain excluded.

Verified locally before resuming paid operations:

- Viewer root redirect is client-only, avoiding a build-time redirect response.
- React Router basename and Vite raw base both use `/vfs-link/viewer`; generated asset URLs retain `/vfs-link/viewer/`.
- `build/client/index.html` is now an explicit Docker build assertion.
- Web test suite: 24 files / 103 tests passed; typecheck and lint passed.
- Exact non-root web build and exact ipproxy Docker build arguments passed; the temporary local image was deleted.

The Cloud Run safety configuration remains unchanged from the approved record: request-based billing/CPU throttling, service and revision minimum 0, maximum 12, 1 vCPU/512 MiB, concurrency 8, no tag, and no pre-started capacity. The retained revision count after historical cleanup is 73 and the current serving rollback revision is `vfs-link-file-server-rollback-34ec7c3`; no retained revision has `minScale > 0`.

Seven reconciled legacy sessions are still nonterminal and continue to block a safe Cloud Run cutover; two of the original nine have reached `complete`. They will not be cancelled while valid and progressing. A new release may be built within the already approved 20-minute Cloud Build deadline, but Cloud Run traffic must not change until all legacy sessions are terminal.

Resumed paid-operation bounds: at most one clean Cloud Build for commit `6d6ade3`, platform timeout 1200 seconds, no automatic retry, expected cost approximately TWD 0.99 before free tier and well below the TWD 100 / 60-minute deployment ceiling. Owner: primary Codex agent. Stop on build failure, timeout, unexpected Cloud Run cost setting, or legacy-drain regression.

Cloud Build started at `2026-08-09T14:26:11Z` from a clean `git archive` of `6d6ade3`:

- Build ID: `dce2248d-6b53-4ff7-a841-3aeb80479fdf`
- Image: `asia-east1-docker.pkg.dev/storage-403503/vfs-link/file-server:git-6d6ade3-20260809t1427z`
- Platform timeout: 1200 seconds
- Retry limit: 0 automatic retries

Cloud Build completed successfully at `2026-08-09T14:30:02Z` (about 3 minutes 49 seconds). Immutable image digest: `sha256:9605e47c9b2bf7876eb48633c234020890d2f38bfbdef9ff07a74f961b6f0444`. No Cloud Run revision or instance was created by the build.

ipproxy deployment result:

- Clean remote Docker build completed within the 20-minute limit; immutable image ID `sha256:9c901cdd336502323ceeb3b6abb575f0d3b9c6760556fc4edff825ad7d2688df`.
- The first startup failed closed on two legacy PostgreSQL upload records. Rollback automatically restored the prior digest and returned healthy.
- Both blockers were verified as expired by 1–6 days, zero-byte, with no local staging file. They were cancelled through the previous release API (HTTP 204), not by direct database mutation.
- The second rollout became healthy on the new immutable image. Active database remains `vfs_link_ipproxy`; nonterminal upload count is 0.
- Viewer and status endpoints return HTTP 200; generated Viewer HTML contains `/vfs-link/viewer/`.
- `physical-health --fail-on-unhealthy`: 2767/2767 healthy, 31.3 GiB, 0 unhealthy.
- Cross-protocol fixture passed: FTP upload → HTTP rename → FTP byte-identical download → FTP delete-to-trash → HTTP permanent delete. Fixture and temporary files were removed.

GCP remains intentionally unmodified after the successful image build. At the latest check, 3 of 9 reconciled legacy sessions are complete and 6 remain nonterminal. The six remaining sessions contain approximately 5.16 GiB of partial payload in aggregate and showed no offset movement for about 30 minutes. Their expirations are between `2026-08-09T17:09:24Z` and `2026-08-09T17:45:56Z`; cancelling them would discard partial upload work and is not authorized by the deployment approval.

Latest Cloud Run read-only audit while blocked: 74 retained revisions, all Ready; `minScale > 0` count 0. `vfs-link-file-server-rollback-34ec7c3` remains latestReady/latestCreated and receives 100% traffic with no tag, 1 vCPU/512 MiB, CPU throttling, min 0, max 12, concurrency 8, timeout 3600, automatic scaling. Aggregate persistent capacity remains 0 instances / 0 vCPU / 0 GiB.

## User-authorized GCP continuation — 2026-08-10

The user explicitly authorized cancelling the remaining six partial legacy uploads and immediately deploying GCP. Before deletion, every exact ID was re-read through the previous release API and had naturally reached `expired`; their aggregate committed progress was 5,544,083,456 bytes (about 5.16 GiB). Each exact session was then cancelled through the previous release API and returned HTTP 204.

Fresh mandatory Cloud Run preflight immediately before the resumed deploy:

- 22 retained revisions; all Ready.
- Every revision has `minScale=0`; count with `minScale > 0` is 0.
- No revision has `run.googleapis.com/cpu-throttling=false`; annotations absent on eight old revisions mean the Cloud Run request-based default, while 14 explicitly set `true`.
- No URL tag. `vfs-link-file-server-rollback-34ec7c3` receives 100% traffic and remains latestReady/latestCreated.
- Serving configuration: 1 vCPU, 512 MiB, 0 GPU, CPU throttling, min 0, max 12, concurrency 8, timeout 3600, automatic scaling.
- Traffic/tag-accessible persistent capacity: 0 instances, 0 vCPU, 0 GiB.
- Retained groups: eight 1 vCPU/1 GiB request-based revisions (min 0/max 10), three 1 vCPU/512 MiB request-based revisions (min 0/max 12), and eleven 2 vCPU/2 GiB request-based revisions (min 0/max 12).

Deployment remains bounded to the already built immutable digest `sha256:9605e47c9b2bf7876eb48633c234020890d2f38bfbdef9ff07a74f961b6f0444`: 10-minute Cloud Run deadline, zero automatic retries, no tag, 1 vCPU/512 MiB, service/revision min 0, max 12, CPU throttling. Idle incremental compute cost is zero. The previously verified worst case at 12 fully active instances remains approximately TWD 36/hour, TWD 864/day, and TWD 25,917/30 days; this is request-driven rather than persistent capacity. Stop on any Ready failure, tag, `minScale > 0`, or disabled throttling.

GCP deployment completed successfully:

- Revision `vfs-link-file-server-git-6d6ade3` became Ready and received 100% traffic only after readiness passed.
- Runtime image is the expected immutable digest `sha256:9605e47c9b2bf7876eb48633c234020890d2f38bfbdef9ff07a74f961b6f0444`.
- Production E2E passed: create upload → direct GCS resumable upload → Complete to business terminal `complete` → replay Complete returned 200/complete → list → byte-identical download → trash → permanent delete. The unique fixture `gcp-6d6ade3-20260809192143.txt` was removed.
- Post-deploy audit: 23 retained revisions, all Ready; `minScale > 0` count 0, `cpu-throttling=false` count 0, GPU count 0, tags count 0. Automatic scaling, max 12, current 1 vCPU/512 MiB, concurrency 8, and 100% traffic to the new revision were verified.
- Monitoring for the new revision reported instance count min 0 / max 1 and latest value 0 at `2026-08-09T19:23:00Z`. Billable instance time was 203.2 seconds; CPU allocation time 415.118 vCPU-seconds (including startup boost) and memory allocation time 101.6 GiB-seconds. At the verified list prices and TWD 33/USD assumption, measured rollout compute is approximately TWD 0.34 before requests/network; Cloud Build remains approximately TWD 0.99 before free tier.
- No Cloud Build is running. The exact 6.5 MiB Cloud Build source archive for this release was deleted after successful deployment; it is reproducible from Git commit `6d6ade3`.
- The active Artifact Registry image is about 29,210,265 compressed bytes. Conservative full-layer storage at an unverified USD 0.10/GiB-month would be below TWD 0.09/month before free tier and shared-layer deduplication; exact billing remains subject to Artifact Registry accounting.

Final persistent Cloud Run capacity is zero. The request-based service can incur request-driven compute/network charges only while handling traffic; its idle compute estimate is TWD 0/hour and TWD 0/day. ipproxy remains on its existing host and new immutable local image; no additional cloud instance was created for it.

Final cross-environment recheck observed that ipproxy had subsequently been superseded outside this GCP continuation by healthy image `sha256:fba75310fe1d2f1bba8e0c557a45cae6ef71dfb6f822e62faa7a05065b2b2490`, labeled Git revision `5e2e4784a9c17bb161f896ca5ebbe60cff782066` (`refactor(deploy): 改用 vfs-link hostname 根路徑`). That commit directly descends from deployed Viewer fix `6d6ade3`; the container started at `2026-08-09T17:13:03Z` and returned healthy/HTTP 200. This task did not roll it back.
