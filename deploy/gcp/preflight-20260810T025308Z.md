# Deployment preflight — 2026-08-10T02:53:08Z

Preflight status at execution: COMPLETE. Deployment and acceptance results are appended below.

## Authorization and scope

- User authorization: explicit `部署` request on 2026-08-10.
- Required targets on branch `twkevinzhang`: GCP project `storage-403503` and ipproxy local.
- Release commit: `79e4565409e43392f63215c07f686c7a52efe970` (`fix(upload): 移除啟動時 thumbnail GC 排程`).
- Repository: `/Users/zhangzhenlong/Projects/vfs-link`; branch `twkevinzhang`; clean worktree before this record was created.
- Release type: code-only. Do not change buckets, metadata prefixes, IAM, secrets, Pub/Sub, CORS, CPU, memory, GPU, max instances, concurrency, timeout, database data, or persistent object mounts.
- Do not run `deploy/gcp/deploy-cloud-run.sh`: its defaults do not match the active production v4/scoped configuration and it performs unrelated infrastructure mutations.
- Paid-operation owner: primary Codex agent only. No agent may run a parallel build, deploy, migration, or load test.

## GCP baseline

- Project / region / service: `storage-403503` / `asia-east1` / `vfs-link-file-server`.
- Service Ready: `True`; scaling: automatic.
- Current revision: `vfs-link-file-server-git-6d6ade3`; traffic: 100%; URL tags: none.
- Current immutable image: `asia-east1-docker.pkg.dev/storage-403503/vfs-link/file-server@sha256:9605e47c9b2bf7876eb48633c234020890d2f38bfbdef9ff07a74f961b6f0444`.
- Current target resources: 1 vCPU, 512 MiB, GPU 0, request-based billing / CPU throttling `true`, min 0, max 12, concurrency 8, request timeout 3600 seconds.
- Active metadata configuration: bucket `storage-403503-vfs-link-metadata-v4-2`, prefix `_vfs-link-v4`, mutation mode `scoped`, drift enabled `true`.
- Active object buckets: archive `storage-403503-archive`; thumbnails `storage-403503-vfs-link-thumbnails`.
- Cloud Build queue before deployment: no `QUEUED` or `WORKING` build.

### All retained revisions

Every retained revision was read successfully. `Ready=True`, GPU 0, and min 0 for all 23 revisions. `default(true)` means the CPU-throttling annotation is absent and the Cloud Run request-based default applies. No revision has a URL tag.

| Revision | CPU | Memory | Throttling | Min | Max | Concurrency | Timeout |
|---|---:|---:|---|---:|---:|---:|---:|
| vfs-link-file-server-git-6d6ade3 | 1 | 512Mi | true | 0 | 12 | 8 | 3600 |
| vfs-link-file-server-rollback-34ec7c3 | 1 | 512Mi | true | 0 | 12 | 8 | 3600 |
| vfs-link-file-server-00098-4d2 | 1 | 512Mi | true | 0 | 12 | 8 | 3600 |
| vfs-link-file-server-00097-r76 | 1 | 512Mi | true | 0 | 12 | 8 | 3600 |
| vfs-link-file-server-00096-qfk | 2 | 2Gi | true | 0 | 12 | 8 | 3600 |
| vfs-link-file-server-00095-qcj | 2 | 2Gi | true | 0 | 12 | 8 | 3600 |
| vfs-link-file-server-00094-l58 | 2 | 2Gi | true | 0 | 12 | 8 | 3600 |
| vfs-link-file-server-00093-p8t | 2 | 2Gi | true | 0 | 12 | 8 | 3600 |
| vfs-link-file-server-00092-8v7 | 2 | 2Gi | true | 0 | 12 | 8 | 3600 |
| vfs-link-file-server-00091-6lj | 2 | 2Gi | true | 0 | 12 | 8 | 3600 |
| vfs-link-file-server-00090-r8s | 2 | 2Gi | true | 0 | 12 | 8 | 3600 |
| vfs-link-file-server-00089-csv | 2 | 2Gi | true | 0 | 12 | 8 | 3600 |
| vfs-link-file-server-00088-n62 | 2 | 2Gi | true | 0 | 12 | 8 | 3600 |
| vfs-link-file-server-upload-7c77f20 | 2 | 2Gi | true | 0 | 12 | 8 | 3600 |
| vfs-link-file-server-emergency-zero | 2 | 2Gi | true | 0 | 12 | 8 | 3600 |
| vfs-link-file-server-00008-q2g | 1 | 1Gi | default(true) | 0 | 10 | 8 | 3600 |
| vfs-link-file-server-00007-4sm | 1 | 1Gi | default(true) | 0 | 10 | 8 | 3600 |
| vfs-link-file-server-00006-9jb | 1 | 1Gi | default(true) | 0 | 10 | 8 | 3600 |
| vfs-link-file-server-00005-qf7 | 1 | 1Gi | default(true) | 0 | 10 | 8 | 3600 |
| vfs-link-file-server-00004-hgq | 1 | 1Gi | default(true) | 0 | 10 | 8 | 3600 |
| vfs-link-file-server-00003-wl4 | 1 | 1Gi | default(true) | 0 | 10 | 8 | 3600 |
| vfs-link-file-server-00002-ktl | 1 | 1Gi | default(true) | 0 | 10 | 8 | 3600 |
| vfs-link-file-server-00001-vsp | 1 | 1Gi | default(true) | 0 | 10 | 8 | 3600 |

Accessible persistent capacity across traffic- or tag-addressable revisions: **0 instances, 0 vCPU, 0 GiB**. Only `vfs-link-file-server-git-6d6ade3` is traffic-addressable and its minimum is 0.

## ipproxy baseline

- SSH alias `ipproxy` is reachable with non-interactive authentication.
- Docker `29.7.1`; Compose `v5.3.1`; external network `services` exists.
- Runtime directory: `/home/ipproxy/vfs-link-ipproxy`.
- Current container `vfs-link-ipproxy`: running and healthy.
- Current immutable image ID: `sha256:fba75310…` (full value will be saved before rollout).
- Current image revision label: `5e2e4784a9c17bb161f896ca5ebbe60cff782066`.
- Current build args: `VITE_BASE_PATH=/`; `VITE_API_BASE_URL=`.
- Active database name: `vfs_link_ipproxy`; read-only query found zero nonterminal uploads.
- Filesystem: 1.8 TiB total, 190 GiB used, 1.6 TiB available (11%).
- PostgreSQL and bind-mounted object/thumbnail data are out of scope and must not be recreated or modified by deployment.

## Cost and duration boundary

Pricing checked 2026-08-10:

- Cloud Run request-based list rate used for the bound: USD 0.000024/vCPU-second, USD 0.0000025/GiB-second, USD 0.40/million requests. Source: <https://cloud.google.com/run/pricing>.
- Cloud Build Taiwan default-pool `e2-standard-2`: USD 0.006/build-minute. Source: <https://cloud.google.com/build/pricing-update>.
- Artifact Registry: first 0.5 GiB-month free per billing account, then USD 0.10/GiB-month. Source: <https://cloud.google.com/artifact-registry/pricing>.
- FX: Bank of Taiwan USD spot sell 32.25 TWD/USD at 2026-08-10 10:42 Asia/Taipei. Source: <https://rate.bot.com.tw/xrt?Lang=en-US>.

Cloud Run target full-active upper bound:

`12 × 3600 × (1 × 0.000024 + 0.5 × 0.0000025) = USD 1.0908/hour = TWD 35.18/hour`.

- 1 hour: TWD 35.18.
- 24 hours: TWD 844.32.
- 30 days: TWD 25,329.60.
- These are request-driven, all-12-instances-continuously-active upper bounds, not persistent idle cost.
- Persistent idle compute with request-based billing and min 0: TWD 0/hour, TWD 0/day, TWD 0/30 days.
- Requests: TWD 12.90/million before free-tier effects.

Incremental deployment bound:

- One Cloud Build, 20-minute hard timeout, zero automatic retries: USD 0.12 = TWD 3.87 worst case; recent expected duration 4–5 minutes gives TWD 0.77–0.97.
- Cloud Run candidate/rollout, 10-minute hard observation per mutation: conservative all-12-active upper bound TWD 5.86; no load test, so actual use should be far lower.
- Combined bounded build plus rollout upper bound: TWD 9.73, below the default TWD 100 authorization ceiling.
- One additional approximately 30 MiB Artifact Registry image: at most about TWD 0.10/month if the account is already above the free 0.5 GiB; exact billable account-wide storage is not available in this preflight.

## Execution limits and stop conditions

- Build source: clean `git archive` of exact release commit `79e4565409e43392f63215c07f686c7a52efe970`.
- Cloud Build: one invocation, platform timeout 1200 seconds, client observation deadline 1200 seconds, retry count 0. Cancel at deadline.
- Cloud Run candidate: exact immutable digest, no traffic, no URL tag, 1 vCPU, 512 MiB, GPU 0, CPU throttling, min 0, max 12, concurrency 8, request timeout 3600 seconds. Mutation deadline 10 minutes; retry count 0.
- GCP paid-operation window: maximum 45 minutes; total paid-operation window across deployment: maximum 60 minutes.
- ipproxy Docker build: exact release archive, 20-minute hard timeout, retry count 0.
- Acceptance uses three small unique fixtures only. It is a bounded production regression check, not a load test. Fixtures must be permanently deleted after byte-identical download and completion/idempotency checks.
- Stop immediately before any new mutation if: any revision min is greater than 0; CPU throttling is false; scaling is manual/fixed; an unexpected tag exists; CPU, memory, GPU, or max would increase; current v4/scoped/drift environment changes; retained revisions cannot all be read; build/deploy reaches its deadline; cost estimate exceeds TWD 100; or nonterminal uploads make rollout unsafe.

## Rollback and cleanup

- GCP rollback target: route `vfs-link-file-server-git-6d6ade3=100`, then re-audit every revision. Candidate must have 0 traffic, no tag, min 0 and CPU throttling before deletion. Delete the new Artifact Registry image only after confirming no revision references its digest.
- ipproxy rollback target: the full immutable pre-deployment image ID recorded immediately before rollout. Re-run Compose with that ID, restore the protected `.env` copy, and repeat health/status/physical-health checks.
- If only one environment succeeds and the other cannot be completed inside the bounded window, roll the successful environment back to avoid a long-lived version mismatch.
- Cleanup: cancel any unfinished build at deadline; remove clean source temp directories; ensure no `QUEUED`/`WORKING` Cloud Build; remove candidate tags (none planned); permanently delete all test fixtures; stop no service or database; keep old immutable images only as rollback artifacts.

## Planned post-deployment acceptance

1. Full Cloud Run all-revision safety audit and exact-revision 100% traffic verification.
2. GCP status check, three concurrent small uploads, terminal `status=complete` and `completionStatus=complete`, idempotent completion, list, byte-identical download, trash, permanent delete.
3. ipproxy exact-image/OCI-label/DB-name checks, status, physical-health, same-origin API check, and the same bounded three-upload lifecycle.
4. Observe each new runtime for more than 10 minutes and confirm logs contain no automatic thumbnail GC start/timer/cleanup execution.
5. Confirm no build remains active, all fixtures are removed, all revisions have min 0 and CPU throttling, no URL tags exist, and persistent Cloud Run capacity remains zero.

## Deployment result — 2026-08-10

Status: DEPLOYMENT AND ACCEPTANCE COMPLETE ON BOTH TARGETS

### Local release gate

- `pnpm run check`: PASS in 34.84 seconds; Web typecheck/lint/boundaries, 25 test files / 109 tests, production Web build, and the Go checks all passed.
- `go test ./cmd/file-server -run '^TestProductionDoesNotScheduleThumbnailGarbageCollection$' -count=1`: PASS.
- `go test ./...`, `go build ./...`, `go vet ./...`, and `git diff --check`: PASS.
- The exact release commit remained `79e4565409e43392f63215c07f686c7a52efe970` throughout the release gate.

### GCP build and rollout

- Cloud Build ID: `0f4e339a-9cea-4c2c-af95-9838bda27fa0`.
- Build start / finish: `2026-08-10T02:54:35.929Z` / `2026-08-10T02:58:53.288Z` (about 4 minutes 17 seconds).
- Build status: SUCCESS; platform timeout remained 1200 seconds; no retry was used.
- Image tag: `asia-east1-docker.pkg.dev/storage-403503/vfs-link/file-server:git-79e4565-20260810t0256z`.
- Immutable image: `asia-east1-docker.pkg.dev/storage-403503/vfs-link/file-server@sha256:c883f0c4223bed95d14501ba177c96276b21697af66717d8211184f489c51d46`.
- Fresh all-revision audit after the build and before revision creation remained safe: 23 revisions, no min/throttling/GPU/Ready/tag violations.
- Candidate `vfs-link-file-server-git-79e4565` was created with no traffic and no tag. It was Ready with 1 vCPU, 512 MiB, GPU 0, CPU throttling true, min 0, max 12, concurrency 8, and timeout 3600 seconds.
- Old/new environment arrays had the same SHA-256 hash `d326ad44b53c29addc6f31f479657df53a89c1954e0f2caab89af535f57d914f`; the runtime service account and active v4/scoped/drift configuration were unchanged.
- Traffic switched only after the candidate audit. `vfs-link-file-server-git-79e4565` is Ready and receives exactly 100%; URL tag count remains 0.
- Startup began `2026-08-10T03:00:20Z`; the HTTP listener became ready at `2026-08-10T03:01:54Z`. Logs contain no automatic thumbnail GC start/timer/cleanup message.
- Authenticated `/api/status`: HTTP 200.

GCP regression acceptance:

- Three unique small fixtures uploaded concurrently through three separate sessions. All three direct GCS final PUTs returned HTTP 200.
- Each original Complete returned business terminal `status=complete` and `completionStatus=complete`; each replay Complete also returned terminal complete.
- All three downloads were byte-identical to their sources.
- The six acceptance Complete requests returned HTTP 200 in approximately 2.34–6.47 seconds. None remained at 100% or pending.
- Fixture root `vfs-link-acceptance/gcp-79e4565-20260810t0304z` was trashed and permanently deleted. The delete operation briefly retried `metadata changed concurrently`, then reached terminal `completed` with progress 3 and no error. A final trash query found zero matching entries.

GCP post-audit:

- 24 retained revisions; all Ready; all min 0; no CPU-throttling false; no GPU; no URL tags.
- Current revision/image/resources: exact `git-79e4565` / `sha256:c883f0…51d46`, 1 vCPU, 512 MiB, min 0, max 12, CPU throttling true, concurrency 8, timeout 3600 seconds.
- Traffic/tag-addressable persistent capacity remains 0 instances / 0 vCPU / 0 GiB.
- No `QUEUED` or `WORKING` Cloud Build remains.
- Monitoring data available through approximately 03:29Z: instance count maximum 2, latest idle count 0; billable instance time about 498.42 seconds; CPU allocation about 713.70 vCPU-seconds; memory allocation about 249.21 GiB-seconds. The latest active count was 2 because acceptance/status and external requests were still being served; this is request-driven and not persistent minimum capacity.
- Measured rollout compute estimate: `713.70 × USD 0.000024 + 249.21 × USD 0.0000025 = about USD 0.01775 = TWD 0.57`, before request/network/free-tier effects.
- Measured Cloud Build estimate: about `4.29 × USD 0.006 = USD 0.0257 = TWD 0.83`, before free-tier effects.
- Combined measured compute/build estimate: about TWD 1.40, below the TWD 9.73 conservative deployment bound and TWD 100 ceiling.

GCP cleanup:

- The exact 3,697,956-byte Cloud Build source object `gs://storage-403503_cloudbuild/source/1786330471.075621-67dc85623fb74e4680f709876f4ffd59.tgz#1786330474454421` was deleted after the successful deployment; it is reproducible from Git commit `79e4565`.
- Local temporary source `/tmp/vfs-link-gcp-build.nHr0Ig` was deleted after the successful deployment.
- No candidate tag, failed revision, running build, load generator, test fixture, or maintenance resource remains.

### ipproxy rollout

- Pre-deploy container: running/healthy, immutable image `sha256:fba75310fe1d2f1bba8e0c557a45cae6ef71dfb6f822e62faa7a05065b2b2490`, revision label `5e2e4784a9c17bb161f896ca5ebbe60cff782066`.
- Pre-deploy database: `vfs_link_ipproxy`; nonterminal uploads: 0; `physical-health --fail-on-unhealthy`: exit 0.
- Exact release archive was copied to `/home/ipproxy/vfs-link-ipproxy/build/79e4565409e43392f63215c07f686c7a52efe970`.
- The single Docker build completed inside its 1200-second platform timeout despite unrelated host build load. No retry or additional concurrency was used.
- New immutable image ID: `sha256:a0218299cdf78ac678acdea83a689b02c61e72e28ea3c09c5360e1057dcd4607`; OCI revision label exactly `79e4565409e43392f63215c07f686c7a52efe970`.
- Static artifact check: root `/assets/` references present; legacy `/vfs-link` prefix absent.
- Compose rollout became healthy within its 300-second wait limit. Current container uses the exact new image ID and returned HTTP 200 from `/api/status`.
- Post-rollout `physical-health --fail-on-unhealthy`: exit 0; active database remains `vfs_link_ipproxy`; nonterminal uploads remain 0.
- `.env` now points to the exact new image ID. Rollback copy: `/home/ipproxy/vfs-link-ipproxy/.env.before-79e4565-20260810t0326z`; the old image remains locally available.

ipproxy regression acceptance:

- Three unique small fixtures uploaded concurrently through three sessions; all final PUTs returned HTTP 200.
- Original and replay Complete calls all returned business terminal complete.
- All three downloads were byte-identical.
- Fixture root `vfs-link-acceptance/ipproxy-79e4565-20260810t0329z` was trashed and permanently deleted synchronously; cleanup PASS.
- After more than ten minutes of container uptime, the container remained running/healthy on the exact new image, `/api/status` returned HTTP 200, `physical-health --fail-on-unhealthy` returned exit 0, database `vfs_link_ipproxy` had zero nonterminal uploads, and logs had zero automatic thumbnail GC or fatal/panic matches.

### Final cross-environment re-audit

- GCP: 24/24 retained revisions Ready; min violations 0; CPU-throttling violations 0; GPU violations 0; tags 0; active builds 0; automatic thumbnail GC log matches 0. Exact new revision remains at 100% traffic with 1 vCPU, 512 MiB, min 0, max 12, and CPU throttling.
- ipproxy: exact release image and OCI revision label remain active and healthy after the ten-minute observation; nonterminal uploads 0; test fixtures 0; automatic thumbnail GC log matches 0.
- No rollback was required. Both deployed targets run release commit `79e4565409e43392f63215c07f686c7a52efe970`.

### Continuing cost after rollout

- Cloud Run persistent idle compute: TWD 0/hour and TWD 0/day because service/revision minimums are all zero and request-based billing/CPU throttling is enforced.
- Request-driven Cloud Run compute, requests, networking, and the existing GCS/metadata services continue to incur normal production usage costs; this deployment did not add persistent capacity.
- One approximately 30 MiB Artifact Registry image remains as the active release. Conservative storage is below about TWD 0.10/month if the billing account is already above the shared 0.5 GiB free tier; exact account-wide storage billing remains unavailable.
- ipproxy uses its existing host, PostgreSQL, and bind-mounted storage. The new local Docker image and retained old rollback image do not create a new cloud instance or a newly authorized recurring cloud charge.
